@model List<AvaliacaoModel>;

@{
    ViewData["Title"] = "Lista de Avaliacaos";
}

<div class="text-center">
    <h1>Lista de Avaliacaos</h1>
</div>

    @if(TempData["MensagemSucesso"] != null){
        <div class="alert alert-success" role="alert">
            <button type="button" class="btn btn-success btn-sm close-alert" arial-label="Close">X</button>
            @TempData["MensagemSucesso"]
        </div>
    }

    @if(TempData["MensagemErro"] != null){
        <div class="alert alert-danger" role="alert">
            <button type="button" class="btn btn-danger btn-sm close-alert" arial-label="Close">X</button>
            @TempData["MensagemErro"]
        </div>
    }

    <br />
    <div class="text-right">
        <a class="btn btn-success" role='button' asp-controller="Avaliacao" asp-action="Criar">Adicionar</a>
    </div>
    <br />

    <table class="table" id="myTable">
      <thead>
        <tr>
          <th scope="col">#</th>
          <th scope="col">Valor</th>
          <th scope="col">Texto</th>
          <th scope="col">Id Chamado</th>
          <th scope="col">Data de Criação</th>
          <th scope="col">Data de Atualização</th>
          <th scope="col"></th>
        </tr>
      </thead>
      <tbody>
        @if(Model != null && Model.Any())
        {
            foreach(AvaliacaoModel avaliacao in Model)
            {
                <tr>
                  <th scope="row">@avaliacao.Id</th>
                  <td>@avaliacao.Valor</td>
                  <td>@avaliacao.Texto</td>
                  <td>@avaliacao.IdChamado</td>
                  <td>@avaliacao.DataCriacao</td>
                  <td>@avaliacao.DataAtualizacao</td>
                  <td>
                       <div class="btn-group" role="group">
                         <a role="button" class="btn btn-primary" asp-route-id="@avaliacao.Id" asp-controller="Avaliacao" asp-action="Editar">Editar</a>
                         <a role="button" class="btn btn-danger" asp-route-id="@avaliacao.Id" asp-controller="Avaliacao" asp-action="ApagarConfirmacao">Excluir</a>
                       </div>
                  </td>
                </tr>
            }
        }
        
        
      </tbody>
    </table>
